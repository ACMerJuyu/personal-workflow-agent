# Optional OpenAI planner configuration.
# Leave OPENAI_API_KEY empty to use the deterministic rule-based planner.
# Set OPENAI_API_KEY to enable the OpenAI Responses API tool-calling planner.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Local development defaults.
APP_HOST=127.0.0.1
APP_PORT=8010
